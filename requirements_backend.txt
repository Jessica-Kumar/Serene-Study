# -------------------------------------------------------
# Backend Requirements — FastAPI on HuggingFace Spaces
# NOTE: torch is installed separately in Dockerfile
#       using the CPU-only index URL
# -------------------------------------------------------
fastapi>=0.103.0
uvicorn>=0.23.0
transformers>=4.33.0
pydantic>=2.0.0
